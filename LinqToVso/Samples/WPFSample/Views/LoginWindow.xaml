<Window x:Class="WPFSample.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LoginWindow" Height="300" Width="500"       
        DataContext="{Binding LoginViewModel, Source={StaticResource ViewModelLocator}}">
    <Grid>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">

            <StackPanel Orientation="Horizontal" Margin="0 0 0 20">
                <TextBlock Text="http://" />
                <TextBox Text="{Binding Account, Mode=TwoWay}" Width="300" />
                <TextBlock Text=".visualstudio.com" />
            </StackPanel>

            <StackPanel Margin="0 0 0 20">
                <TextBlock Text="Email" />
                <TextBox Text="{Binding Email, Mode=TwoWay}" />
            </StackPanel>
            <StackPanel Margin="0 0 0 20">
                <TextBlock Text="Pasword" />
                <TextBox Text="{Binding Password, Mode=TwoWay}" />
            </StackPanel>

            <Button Content="Login" Command="{Binding LoginCommand}" />
        </StackPanel>
    </Grid>
</Window>
